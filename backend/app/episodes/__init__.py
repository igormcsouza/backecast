"""Episodes domain: router (HTTP), service (business logic),
repository (DynamoDB access), schemas (request/response models),
exceptions (domain errors). One vertical slice per domain, not
layered by file type — see manual.md §5.
"""
